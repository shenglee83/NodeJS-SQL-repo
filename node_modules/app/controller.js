app.controller("mainController", function($scope, myFactory) {
    $scope.modelData = {
        name: "RIck"
    };
    $scope.modelData.productData = [];
    $scope.modelData.reviewData = [];
    myFactory.getReviewFactory($scope);
    $scope.getProduct = function() {
        console.log("In button function");
        myFactory.getProductFactory($scope);
        console.log($scope.modelData.productData);
    };


    console.log("Testing");
})
